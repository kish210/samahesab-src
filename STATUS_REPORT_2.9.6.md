# گزارشِ وضعیتِ فنیِ سما حساب — نسخهٔ ۲.۹.۶

> تاریخِ تدوین: ۱۴۰۵/۰۵/۱۳ (2026-08-04) — برایِ بررسیِ تیمِ فنی. منبع: `todo.rm` (تاریخچهٔ کاملِ
> کارها)، `git log`، و بازرسیِ مستقیمِ DB/کد در زمانِ نگارش. هر بندِ زیر یا **تأییدشده با تستِ
> زنده** است یا صریحاً به‌عنوانِ **محدودیتِ صادقانه/تست‌نشده** علامت خورده — چیزی ادعا نشده که
> بررسی نشده باشد.

---

## ۱. خلاصهٔ اجرایی

- **نسخهٔ فعلی:** ۲.۹.۶ — نصابِ `SamaHesab_Web_Setup_v2.9.6.exe` (~۷۸ مگابایت)، تنها نصابِ منتشرشده
  از v2.9 به بعد (سرور+وب، بدونِ دسکتاپِ WPF جدا — کاربر از مرورگر رویِ `http://<server>:5080/web/`
  کار می‌کند). دسکتاپِ WPF هنوز **در کد** کامل و build-سبز است ولی دیگر نصابِ جدا برایش منتشر
  نمی‌شود (تصمیمِ آگاهانه، مستند در CLAUDE.md).
- **پشتهٔ فنی:** .NET 9 / ASP.NET Core API + EF Core (SQL Server) در بک‌اند؛ CQRS با MediatR؛
  React 18 + TypeScript + Vite در کلاینتِ وب؛ WPF (.NET 9) در کلاینتِ دسکتاپِ آرشیوی.
- **تست:** **۸۲۳/۸۲۳ تستِ backend سبز** در زمانِ نگارشِ این گزارش (`dotnet test`، بدونِ skip).
  کلاینتِ وب تستِ خودکار ندارد (فقط `tsc` type-check + build سبز + تستِ زندهٔ دستی رویِ DBِ واقعی
  در هر فیچر — به‌صورتِ سیستماتیک در `todo.rm` مستند شده).
- **معماری:** مونولیت با مسیرِ مهاجرت به ماژولار — `SamaHesab.Modules.Abstractions`/`IModule` از
  قبل جاافتاده؛ ۸ ماژولِ اختیاری (POS/رستوران/هتل/گردشگری/HR/حضوروغیاب/پیمانکاری/مودیان) از هستهٔ
  حسابداری/انبار/فروش/خرید/امنیت جدا شده‌اند و به‌صورتِ `.mspkg` قابلِ نصب/حذفِ مستقل‌اند.

---

## ۲. وضعیتِ هستهٔ سیستم (Core)

| بخش | وضعیت | یادداشت |
|---|---|---|
| حسابداری (سند/دفترِکل/تراز/ترازنامه/سودوزیان) | ✅ کامل، تست‌شده | صفحه‌بندیِ DB-level برایِ گزارش‌هایِ بزرگ (U-DB-PAGING) |
| خزانه (دریافت/پرداخت/چک/چندبانکی) | ✅ کامل | UI منتقل شد به کارتِ شخص (خزانه از منو حذف شد، منطقِ backend دست‌نخورده) |
| انبار (چندانباره، FIFO، Landed Cost، حسابِ per-warehouse) | ✅ کامل | بچ/سریال هنوز فقط در دسکتاپ پورت شده، وب ندارد (پایین) |
| فروش/خرید (فاکتور، مرجوعی، کنسینمنت) | ✅ کامل | تسویهٔ کنسینمنت تست‌شده رویِ DBِ واقعی |
| اشخاص/CRM (مشتری، تأمین‌کننده، دفترِ معینِ append-only) | ✅ کامل | `Party.Balance` از ۲۰۲۶-۰۷-۱۷ محاسبه‌شده از `PartyLedgerEntry` است، نه فیلدِ مستقلِ ناهماهنگ |
| امنیت/RBAC (نقش/مجوز/کاربر/لاگِ حسابرسی) | ✅ کامل در وب | پنلِ نقش/مجوز/تخصیص + لاگِ حسابرسی؛ **دسکتاپ هنوز TextBlockِ استاب است** (`CompanySettingsView`/`UserManagementView`) |
| چندشعبه‌ای | ✅ کامل | فیلترِ `BranchId` رویِ کالا/انبار/کارمند + گزارشِ per-branch |
| لایسنس | ✅ کامل، per-module | RSA-signed، بدونِ نیازِ تغییرِ schema برایِ entitlement per-tier |
| ویزاردِ راه‌اندازیِ اولیه | ✅ کامل، هم‌دسکتاپ‌هم‌وب | وب تازه اضافه شد (v2.9.6) — ۵ گام، بنرِ غیرِاجباری در Shell |

---

## ۳. وضعیتِ ماژول‌هایِ اختیاری

| ماژول | Backend | UIِ وب | UIِ دسکتاپ | تستِ زندهٔ رویِ سخت‌افزار/سرویسِ واقعی |
|---|---|---|---|---|
| POS | ✅ | ✅ کامل (سبد/Hold-Recall/شیفت/بارکد) | ✅ | ✅ (چاپِ رسید تا دیالوگِ Print تست شد، بدونِ پرینترِ فیزیکی) |
| رستوران | ✅ | ✅ (میز/سالن/آشپزخانه/مالیات‌خدمات/بیرون‌بر) | ✅ | ⚠️ تب‌استریپِ کاملِ MDI هنوز پورت نشده (پایین) |
| هتل (PMS) | ✅ | ✅ | ✅ | — |
| گردشگری | ✅ | ✅ | ✅ | — |
| HR/حقوق‌ودستمزد | ✅ | ✅ | ✅ | ⚠️ `GetSalarySlipsQuery` (پیش‌نمایشِ فیشِ حقوق) هنوز پورت نشده |
| حضوروغیاب | ✅ | ✅ | ✅ | ❌ **هرگز رویِ دستگاهِ فیزیکیِ زدکتکو تست نشده** — پروتکل از مهندسیِ معکوسِ غیررسمی است؛ ریسکِ ناسازگاریِ فرمت بینِ مدل‌ها |
| پیمانکاری | ✅ | ✅ | ✅ | — |
| پشتیبانی (تیکت) | ✅ | ✅ | ✅ | — |
| سامانهٔ مودیان (صورتحسابِ الکترونیکی) | ✅ (رمزنگاریِ JWS/JWE با تستِ round-trip) | ✅ | — | ⚠️ **بدونِ اعتبارنامهٔ Sandbox/واقعیِ سازمانِ مالیاتی، تأییدنشده که سرورِ واقعی می‌پذیرد** — فقط ساختارِ محلی/رمزنگاری آزموده شده |
| باشگاهِ مشتریان (CRM/امتیاز) | ✅ | — | ✅ | ماژولِ کوچک، فقط دسکتاپ |

---

## ۴. وضعیتِ کلاینتِ وب (پوششِ صفحات)

پوششِ صفحات نسبت به دسکتاپ **کامل** است برایِ هستهٔ ERP + همهٔ ماژول‌هایِ فوق. موارد آگاهانه
پورت‌نشده (تصمیمِ صریح، نه فراموشی):

- **BatchSerial UI** (تب‌هایِ بچ/سریال + هشدارِ انقضا) — backend دارد، وب نه.
- **۶ داشبوردِ نقشیِ جداگانه** (مدیر/حسابدار/انباردار/صندوقدار/رستوران/مالک) — عمداً به نفعِ
  `DashboardPage`/`AnalyticsPage`ِ عمومی حذف شدند تا تکرارِ UI نباشد.
- **درایورِ کارت‌خوانِ سخت‌افزاری** در POSِ وب — بلاک‌شده تا فراهم‌شدنِ SDK/سخت‌افزارِ واقعی.

---

## ۵. باگ‌هایِ واقعیِ کشف‌وحل‌شده (تاریخچهٔ فنی — نشانهٔ سلامتِ فرایندِ تست)

فهرستِ زیر، باگ‌هایی هستند که در حینِ **تستِ زندهٔ رویِ DBِ واقعی** پیدا شدند (نه در بازرسیِ کد)
و رفع شدند — برایِ آگاهیِ تیم از الگوهایِ خطایِ پرتکرار:

1. **ناسازگاریِ کلیدِ JWT بینِ صدور/تأیید** — در Production با کلیدِ کوتاه، `Program.cs` کلیدِ
   جایگزین برایِ تأیید می‌ساخت ولی `JwtTokenService.Issue` با کلیدِ دیگری امضا می‌کرد → هر لاگین
   ۴۰۱. رفع با `JwtSettings` singleton.
2. **تاریخِ میلادی به‌جایِ شمسی از کلاینتِ وب** — ۴ فرم (چک/خزانه/فاکتورِ فروش/خرید) تاریخِ
   `toISOString()` (میلادی) می‌فرستادند درحالی‌که سرور فقط رشتهٔ شمسیِ `yyyy/MM/dd` را می‌فهمد —
   یعنی هر فاکتورِ ثبت‌شده از وب تاریخِ نامعتبر می‌گرفت و از فیلترِ گزارش‌ها بیرون می‌افتاد.
3. **ناهماهنگیِ GL/Kardex در فاکتورِ خرید** — سندِ حسابداری کلِ مبلغ (کالا+مالیات+حمل) را یک‌جا
   بدهکارِ موجودی می‌زد، درحالی‌که Kardex فقط قیمتِ خام (بدونِ مالیات) را حساب می‌کرد → ماندهٔ GL
   همیشه از ارزشِ واقعیِ انبار جلوتر می‌افتاد.
4. **دورریختنِ بی‌صدایِ مازادِ پرداخت** — تخصیصِ پرداخت به فاکتورهایِ باز، مازادِ تخصیص‌نیافته را
   بدونِ ثبت دور می‌ریخت (نه پیش‌دریافت، نه خطا) — پول به‌صورتِ نامرئی گم می‌شد.
5. **کلیدِ تکراریِ شمارهٔ فاکتورِ کنسینمنت** — کنسینمنت پیشوندِ "F" را با فروشِ عادی مشترک
   می‌گرفت (هر دو "F000001" تولید می‌کردند).
6. **`AmbiguousMatchException` پنهان در گزارش‌ها** — هنگامِ افزودنِ اندپوینتِ جدید مشخص شد
   مسیرِ مشابه از قبل در کنترلرِ دیگری وجود داشت؛ فقط با اجرایِ واقعی (نه بازرسیِ کد) کشف شد.
7. **بلوکِ امضایِ چاپِ فاکتور** — کلاسِ CSSِ `.print-only` مقدارِ `display:block!important`
   می‌گذاشت که `display:flex` را می‌شکست → امضایِ فروشنده/خریدار زیرِ هم می‌افتاد به‌جایِ کنارِ هم.
8. **باگِ closureِ ماشین‌حسابِ توپ‌بار** — عملیاتِ زنجیره‌ای (`1250000 + 9%`) با `useState` معمولی
   رویِ state کهنه محاسبه می‌شد (نتیجهٔ غلط)؛ رفع با `useReducer`.

---

## ۶. محدودیت‌هایِ صادقانهٔ بازِ فعلی (ریسک‌هایِ شناخته‌شده)

> این‌ها **آگاهانه** به کاربر/تیم اعلام شده‌اند، نه پنهان‌شده.

| مورد | ریسک | وضعیت |
|---|---|---|
| ماژولِ حضوروغیاب/دستگاهِ زدکتکو | متوسط–بالا | هرگز رویِ سخت‌افزارِ واقعی تست نشده؛ فرمتِ رکوردِ تردد بینِ مدل‌ها می‌تواند فرق کند |
| ماژولِ مودیان | بالا (قانونی/مالیاتی) | بدونِ اعتبارنامهٔ Sandbox واقعی، پذیرشِ سازمانِ مالیاتی تأییدنشده |
| CI/GitHub Actions | پایین–متوسط | از ۲۰۲۶-۰۷-۰۱ هیچ runی ثبت نشده — به‌احتمالِ زیاد مسئلهٔ تعلیقِ اکانتِ گیت‌هابِ `kish210`، نه خرابیِ workflow |
| یکدستیِ کلیدهایِ F در دسکتاپ | پایین (UX) | معنایِ کلیدها بینِ صفحات فرق دارد (مثلاً F7=«افزودنِ ردیف» جایی، «ذخیره» جای دیگر) — عمداً دست‌نخورده تا کاربرانِ فعلی گیج نشوند |
| کارت‌خوانِ سخت‌افزاریِ POS | بلاک‌شده | نیازِ دستگاه/SDKِ واقعی |
| `CompanySettingsView`/`UserManagementView` دسکتاپ | متوسط | هنوز فقط TextBlockِ «در حالِ توسعه»، حتی mock ViewModel هم ندارند (معادلِ وبشان کامل است) |
| یادآورِ پیامکی | آماده، غیرفعال | کد/UI/۳ providerِ SMS کامل سیم‌کشی شده‌اند؛ فقط منتظرِ اعتبارنامهٔ providerِ واقعی از کاربر |

---

## ۷. پاک‌سازیِ دادهٔ کشف‌شده در همین بررسی

هنگامِ رسیدگی به یادداشتِ قدیمیِ «دو انبار با نامِ یکسان (Id=1,2)»، بررسیِ مستقیمِ DB نشان داد
ردیفِ Id=2 (`Code=W002`, `IsDefault=0`, بدونِ موجودی/تراکنش) دادهٔ آزمایشیِ خودِ همین نشست بود
(از تستِ زندهٔ ویزاردِ راه‌اندازی) — **حذف شد**؛ انبارِ واقعیِ سیستم (`Id=1`, `W001`, پیش‌فرض) دست‌نخورده ماند.

---

## ۸. توزیع/نصب

- نصابِ `.exe` با Inno Setup (ISCC) ساخته می‌شود؛ فقط یک نصاب از v2.9 به بعد (سرور+وب).
- `download/` (پوشهٔ لوکالِ گیت‌ایگنورشده) آخرین نصاب‌ها را نگه می‌دارد؛ آپلودِ نهایی به سایت/cPanel
  دستیِ کاربر است.
- ریلیزِ GitHub رویِ مخزنِ عمومیِ `kish210/SamaHesab` (نه سورس) — سورس در `kish210/samahesab-src`
  (خصوصی) نگهداری می‌شود.

---

## ۹. جمع‌بندی برایِ تیمِ فنی

سیستم از نظرِ **هسته** (حسابداری/انبار/فروش/خرید/CRM/امنیت) و **کلاینتِ وب** در وضعیتِ باثبات و
تست‌شده است (۸۲۳ تستِ خودکار + تاریخچهٔ مستندِ تستِ زندهٔ فیچر-به-فیچر رویِ DBِ واقعی). ریسک‌هایِ
اصلیِ باقی‌مانده **سخت‌افزاری/بیرونی**اند (دستگاهِ حضوروغیاب، کارت‌خوانِ POS، اعتبارنامهٔ مودیان)،
نه نقصِ منطقِ نرم‌افزار. مواردِ نیازمندِ تصمیمِ محصولی (نه فنی) در بخشِ ۶ فهرست شده‌اند.
