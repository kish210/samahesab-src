<UserControl x:Class="SamaHesab.WPF.Views.HRM.ShiftManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="{DynamicResource AppBackground}"
             FontFamily="{StaticResource VazirFont}"
             FlowDirection="RightToLeft">
    <UserControl.InputBindings>
        <KeyBinding Key="F2" Command="{Binding NewShiftCommand}"/>
        <KeyBinding Key="F7" Command="{Binding SaveCommand}"/>
        <KeyBinding Key="F5" Command="{Binding RefreshCommand}"/>
    </UserControl.InputBindings>
    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition Width="320"/>
        </Grid.ColumnDefinitions>

        <!-- فهرستِ شیفت‌ها -->
        <Border Grid.Column="0" Style="{StaticResource FormSectionCard}" Padding="0">
            <DataGrid Style="{StaticResource ErpDataGrid}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                      ItemsSource="{Binding Shifts}" SelectedItem="{Binding SelectedShift}"
                      RowHeight="{DynamicResource ErpDensRowHeight}" FontFamily="{StaticResource VazirFont}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="نامِ شیفت" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="شروع" Binding="{Binding Start}" Width="80"/>
                    <DataGridTextColumn Header="پایان" Binding="{Binding End}" Width="80"/>
                    <DataGridTextColumn Header="استراحت(دقیقه)" Binding="{Binding BreakMinutes, Converter={StaticResource Num}}" Width="110" ElementStyle="{StaticResource NumCell}"/>
                    <DataGridTextColumn Header="ساعتِ مؤظف" Binding="{Binding StandardHours, Converter={StaticResource Num}}" Width="100" ElementStyle="{StaticResource NumCell}"/>
                    <DataGridCheckBoxColumn Header="شب‌کاری" Binding="{Binding IsNight}" Width="80"/>
                    <DataGridTemplateColumn Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button ToolTip="حذفِ شیفت" Cursor="Hand" Background="Transparent" BorderThickness="0"
                                        Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}">
                                    <materialDesign:PackIcon Kind="DeleteOutline" Width="18" Height="18" Foreground="{DynamicResource DangerColor}"/>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- فرمِ ویرایش -->
        <Border Grid.Column="2" Style="{StaticResource FormSectionCard}" Padding="12" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="مشخصاتِ شیفت" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
                <TextBlock Text="نامِ شیفت" FontSize="11" Foreground="{DynamicResource SecondaryText}"/>
                <TextBox Height="32" Margin="0,2,0,8" Text="{Binding Name}"/>
                <StackPanel Orientation="Horizontal">
                    <StackPanel Margin="0,0,10,8"><TextBlock Text="شروع" FontSize="11" Foreground="{DynamicResource SecondaryText}"/>
                        <TextBox Width="90" Height="32" Text="{Binding Start}" FlowDirection="LeftToRight"/></StackPanel>
                    <StackPanel Margin="0,0,0,8"><TextBlock Text="پایان" FontSize="11" Foreground="{DynamicResource SecondaryText}"/>
                        <TextBox Width="90" Height="32" Text="{Binding End}" FlowDirection="LeftToRight"/></StackPanel>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <StackPanel Margin="0,0,10,8"><TextBlock Text="استراحت(دقیقه)" FontSize="11" Foreground="{DynamicResource SecondaryText}"/>
                        <TextBox Width="90" Height="32" Text="{Binding BreakMinutes}" FlowDirection="LeftToRight"/></StackPanel>
                    <StackPanel Margin="0,0,0,8"><TextBlock Text="ساعتِ مؤظف" FontSize="11" Foreground="{DynamicResource SecondaryText}"/>
                        <TextBox Width="90" Height="32" Text="{Binding StandardHours}" FlowDirection="LeftToRight"/></StackPanel>
                </StackPanel>
                <CheckBox Content="شیفتِ شب‌کاری" IsChecked="{Binding IsNight}" Margin="0,4,0,8"/>
                <TextBlock Text="یادداشت" FontSize="11" Foreground="{DynamicResource SecondaryText}"/>
                <TextBox Height="50" Margin="0,2,0,10" Text="{Binding Notes}" TextWrapping="Wrap" AcceptsReturn="True"/>
                <StackPanel Orientation="Horizontal">
                    <Button Content="💾 ذخیره (F7)" Style="{StaticResource PrimaryButton}" Height="34" Margin="0,0,8,0" Command="{Binding SaveCommand}"/>
                    <Button Content="➕ جدید (F2)" Style="{StaticResource SecondaryButton}" Height="34" Command="{Binding NewShiftCommand}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
