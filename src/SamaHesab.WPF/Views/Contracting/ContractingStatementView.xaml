<UserControl x:Class="SamaHesab.WPF.Views.Contracting.ContractingStatementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource AppBackground}"
             FontFamily="{StaticResource VazirFont}"
             FlowDirection="RightToLeft">
    <UserControl.InputBindings>
        <KeyBinding Key="F7" Command="{Binding SaveCommand}"/>
        <KeyBinding Key="F9" Command="{Binding PostCommand}"/>
    </UserControl.InputBindings>
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="ثبتِ صورت‌وضعیتِ پیمان" FontSize="18" FontWeight="Bold" Margin="2,0,0,8"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="360"/>
            </Grid.ColumnDefinitions>

            <!-- ورودی‌ها -->
            <Border Grid.Column="0" Style="{StaticResource FormSectionCard}" Padding="12" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="پیمان" FontSize="11" Foreground="{DynamicResource SecondaryText}"/>
                    <ComboBox Height="32" Margin="0,2,0,10" ItemsSource="{Binding Projects}" DisplayMemberPath="Title"
                              SelectedItem="{Binding SelectedProject}"/>
                    <WrapPanel>
                        <StackPanel Margin="0,0,12,8"><TextBlock Text="شمارهٔ صورت‌وضعیت" FontSize="11" Foreground="{DynamicResource SecondaryText}"/>
                            <TextBox Width="90" Height="32" Text="{Binding Number}" FlowDirection="LeftToRight"/></StackPanel>
                        <StackPanel Margin="0,0,12,8"><TextBlock Text="تاریخ" FontSize="11" Foreground="{DynamicResource SecondaryText}"/>
                            <TextBox Width="110" Height="32" Text="{Binding Date}" FlowDirection="LeftToRight"/></StackPanel>
                        <StackPanel Margin="0,0,12,8" VerticalAlignment="Bottom">
                            <CheckBox Content="صورت‌وضعیتِ قطعی" IsChecked="{Binding IsFinal}" VerticalAlignment="Center" Margin="0,14,0,0"/></StackPanel>
                        <StackPanel Margin="0,0,12,8"><TextBlock Text="سالِ مالی" FontSize="11" Foreground="{DynamicResource SecondaryText}"/>
                            <ComboBox Width="130" Height="32" ItemsSource="{Binding FiscalYears}" DisplayMemberPath="Title" SelectedValuePath="Id" SelectedValue="{Binding SelectedFiscalYearId}"/></StackPanel>
                    </WrapPanel>
                    <Separator Margin="0,4,0,10"/>
                    <WrapPanel>
                        <StackPanel Margin="0,0,12,8"><TextBlock Text="کارکردِ تجمعی" FontSize="11" Foreground="{DynamicResource SecondaryText}"/>
                            <TextBox Width="150" Height="32" Text="{Binding CumulativeGrossWork}" FlowDirection="LeftToRight"/></StackPanel>
                        <StackPanel Margin="0,0,12,8"><TextBlock Text="تجمعیِ قبلی" FontSize="11" Foreground="{DynamicResource SecondaryText}"/>
                            <TextBox Width="150" Height="32" Text="{Binding PreviousCumulative}" FlowDirection="LeftToRight"/></StackPanel>
                        <StackPanel Margin="0,0,12,8"><TextBlock Text="تعدیل" FontSize="11" Foreground="{DynamicResource SecondaryText}"/>
                            <TextBox Width="130" Height="32" Text="{Binding AdjustmentAmount}" FlowDirection="LeftToRight"/></StackPanel>
                        <StackPanel Margin="0,0,12,8"><TextBlock Text="مابه‌التفاوتِ مصالح" FontSize="11" Foreground="{DynamicResource SecondaryText}"/>
                            <TextBox Width="130" Height="32" Text="{Binding MaterialDiffAmount}" FlowDirection="LeftToRight"/></StackPanel>
                        <StackPanel Margin="0,0,12,8"><TextBlock Text="جریمه" FontSize="11" Foreground="{DynamicResource SecondaryText}"/>
                            <TextBox Width="120" Height="32" Text="{Binding Penalty}" FlowDirection="LeftToRight"/></StackPanel>
                        <StackPanel Margin="0,0,12,8"><TextBlock Text="سایرِ کسور" FontSize="11" Foreground="{DynamicResource SecondaryText}"/>
                            <TextBox Width="120" Height="32" Text="{Binding Other}" FlowDirection="LeftToRight"/></StackPanel>
                    </WrapPanel>
                </StackPanel>
            </Border>

            <!-- پیش‌نمایشِ زندهٔ آبشار -->
            <Border Grid.Column="2" Style="{StaticResource FormSectionCard}" Padding="14" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="پیش‌نمایشِ آبشار (زنده)" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
                    <DockPanel Margin="0,3"><TextBlock Text="کارکردِ دوره" Foreground="{DynamicResource SecondaryText}"/><TextBlock DockPanel.Dock="Left" Text="{Binding PeriodWork, StringFormat={}{0:#,##0}}" FontWeight="SemiBold" FlowDirection="LeftToRight"/></DockPanel>
                    <DockPanel Margin="0,3"><TextBlock Text="ناخالصِ دوره" Foreground="{DynamicResource SecondaryText}"/><TextBlock DockPanel.Dock="Left" Text="{Binding GrossThisPeriod, StringFormat={}{0:#,##0}}" FontWeight="SemiBold" FlowDirection="LeftToRight"/></DockPanel>
                    <Separator Margin="0,6"/>
                    <DockPanel Margin="0,3"><TextBlock Text="بازیافتِ پیش‌پرداخت" Foreground="{DynamicResource SecondaryText}"/><TextBlock DockPanel.Dock="Left" Text="{Binding AdvanceRecovery, StringFormat={}{0:#,##0}}" FlowDirection="LeftToRight"/></DockPanel>
                    <DockPanel Margin="0,3"><TextBlock Text="حسنِ‌انجام (سپرده)" Foreground="{DynamicResource SecondaryText}"/><TextBlock DockPanel.Dock="Left" Text="{Binding Retention, StringFormat={}{0:#,##0}}" FlowDirection="LeftToRight"/></DockPanel>
                    <DockPanel Margin="0,3"><TextBlock Text="بیمه" Foreground="{DynamicResource SecondaryText}"/><TextBlock DockPanel.Dock="Left" Text="{Binding Insurance, StringFormat={}{0:#,##0}}" FlowDirection="LeftToRight"/></DockPanel>
                    <DockPanel Margin="0,3"><TextBlock Text="مالیات" Foreground="{DynamicResource SecondaryText}"/><TextBlock DockPanel.Dock="Left" Text="{Binding Tax, StringFormat={}{0:#,##0}}" FlowDirection="LeftToRight"/></DockPanel>
                    <Border Background="#EEF2F7" CornerRadius="6" Padding="10,8" Margin="0,8,0,0">
                        <DockPanel><TextBlock Text="خالصِ پرداختی" FontWeight="Bold"/><TextBlock DockPanel.Dock="Left" Text="{Binding NetPayable, StringFormat={}{0:#,##0} ریال}" FontWeight="Bold" FontSize="16" Foreground="{DynamicResource PrimaryAccent}" FlowDirection="LeftToRight"/></DockPanel>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>

        <!-- دکمه‌ها -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="2,10,0,0">
            <Button Content="💾 ذخیرهٔ پیش‌نویس (F7)" Style="{StaticResource SecondaryButton}" Height="38" Margin="0,0,8,0" Command="{Binding SaveCommand}"/>
            <Button Content="✓ قطعی و سند (F9)" Style="{StaticResource PrimaryButton}" Height="38" Command="{Binding PostCommand}" IsEnabled="{Binding CanPost}"/>
        </StackPanel>
    </Grid>
</UserControl>
