<UserControl x:Class="SamaHesab.WPF.Views.Tourism.TourismProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource AppBackground}"
             FontFamily="{StaticResource VazirFont}"
             FlowDirection="RightToLeft">
    <UserControl.InputBindings>
        <KeyBinding Key="F2" Command="{Binding NewCommand}"/>
        <KeyBinding Key="F7" Command="{Binding SaveCommand}"/>
    </UserControl.InputBindings>
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="محصولات و خدماتِ گردشگری" FontSize="18" FontWeight="Bold" Margin="2,0,0,8"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="360"/>
            </Grid.ColumnDefinitions>

            <!-- فهرستِ محصولات -->
            <Border Grid.Column="0" Style="{StaticResource FormSectionCard}" Padding="0">
                <DataGrid Style="{StaticResource ErpDataGrid}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                          ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}"
                          RowHeight="{DynamicResource ErpDensRowHeight}" FontFamily="{StaticResource VazirFont}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="نام" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="تأمین‌کننده" Binding="{Binding SupplierName}" Width="150"/>
                        <DataGridTextColumn Header="بهای خرید" Binding="{Binding PurchasePrice, Converter={StaticResource Num}}" Width="120" ElementStyle="{StaticResource NumCell}"/>
                        <DataGridTextColumn Header="فروشِ پیش‌فرض" Binding="{Binding DefaultSalePrice, Converter={StaticResource Num}}" Width="120" ElementStyle="{StaticResource NumCell}"/>
                        <DataGridCheckBoxColumn Header="فعال" Binding="{Binding Active}" Width="56"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- فرمِ افزودن/ویرایش -->
            <Border Grid.Column="2" Style="{StaticResource FormSectionCard}" Padding="12">
                <StackPanel>
                    <TextBlock Text="مشخصاتِ محصول" FontWeight="Bold" Margin="0,0,0,10"/>

                    <TextBlock Text="نام محصول/خدمت" Foreground="{DynamicResource SecondaryText}"/>
                    <TextBox Height="32" Margin="0,2,0,10" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="تأمین‌کننده" Foreground="{DynamicResource SecondaryText}"/>
                    <ComboBox Height="32" Margin="0,2,0,10" ItemsSource="{Binding Suppliers}" DisplayMemberPath="Name"
                              SelectedValuePath="Id" SelectedValue="{Binding SupplierPartyId}"
                              IsEditable="True" IsTextSearchEnabled="True" TextSearch.TextPath="Name"/>

                    <TextBlock Text="بهای خرید (ریال)" Foreground="{DynamicResource SecondaryText}"/>
                    <TextBox Height="32" Margin="0,2,0,10" FlowDirection="LeftToRight"
                             Text="{Binding PurchasePrice, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="قیمتِ فروشِ پیش‌فرض (ریال)" Foreground="{DynamicResource SecondaryText}"/>
                    <TextBox Height="32" Margin="0,2,0,10" FlowDirection="LeftToRight"
                             Text="{Binding DefaultSalePrice, UpdateSourceTrigger=PropertyChanged}"/>

                    <CheckBox Content="نیاز به لیستِ مسافر (تور/گشت)" Margin="0,2,0,8" IsChecked="{Binding RequiresPassengerList}"/>
                    <CheckBox Content="فعال" Margin="0,0,0,14" IsChecked="{Binding Active}"/>

                    <StackPanel Orientation="Horizontal">
                        <Button Content="➕ جدید (F2)" Style="{StaticResource SecondaryButton}" Height="34" Command="{Binding NewCommand}"/>
                        <Button Content="💾 ذخیره (F7)" Style="{StaticResource PrimaryButton}" Height="34" Margin="8,0,0,0" Command="{Binding SaveCommand}"/>
                        <Button Content="🗑 حذف" Style="{StaticResource SecondaryButton}" Height="34" Margin="8,0,0,0" Command="{Binding DeleteCommand}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
