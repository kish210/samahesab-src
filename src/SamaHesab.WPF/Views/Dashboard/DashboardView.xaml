<UserControl x:Class="SamaHesab.WPF.Views.Dashboard.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             Background="{DynamicResource AppBackground}"
             FontFamily="{StaticResource VazirFont}"
             FlowDirection="RightToLeft">

    <UserControl.Resources>
        <!-- Compact KPI tile -->
        <Style x:Key="KpiTile" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="Padding" Value="10,6"/>
        </Style>
        <Style x:Key="KpiLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryText}"/>
        </Style>
        <Style x:Key="KpiValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,2,0,0"/>
        </Style>
        <!-- Dense list panel -->
        <Style x:Key="ListPanel" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Margin" Value="3"/>
        </Style>
        <Style x:Key="PanelHeader" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource TableHeaderBg}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Padding" Value="10,6"/>
        </Style>
        <Style x:Key="DenseGrid" TargetType="telerik:RadGridView">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="ShowGroupPanel" Value="False"/>
            <Setter Property="RowIndicatorVisibility" Value="Collapsed"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowHeight" Value="26"/>
            <Setter Property="FontFamily" Value="{StaticResource VazirFont}"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>   <!-- KPI strip -->
            <RowDefinition Height="*"/>      <!-- dense lists -->
        </Grid.RowDefinitions>

        <!-- ═══ Compact KPI strip ═══ -->
        <UniformGrid Grid.Row="0" Columns="7">
            <Border Style="{StaticResource KpiTile}">
                <StackPanel><TextBlock Text="فروش امروز" Style="{StaticResource KpiLabel}"/>
                    <TextBlock Text="{Binding TodaySales, StringFormat={}{0:#,##0}}" Style="{StaticResource KpiValue}" Foreground="{DynamicResource PrimaryAccent}"/></StackPanel>
            </Border>
            <Border Style="{StaticResource KpiTile}">
                <StackPanel><TextBlock Text="خرید امروز" Style="{StaticResource KpiLabel}"/>
                    <TextBlock Text="{Binding TodayPurchase, StringFormat={}{0:#,##0}}" Style="{StaticResource KpiValue}" Foreground="{DynamicResource WarningColor}"/></StackPanel>
            </Border>
            <Border Style="{StaticResource KpiTile}">
                <StackPanel><TextBlock Text="سود خالص (ماه)" Style="{StaticResource KpiLabel}"/>
                    <TextBlock Text="{Binding NetProfit, StringFormat={}{0:#,##0}}" Style="{StaticResource KpiValue}" Foreground="{DynamicResource SuccessColor}"/></StackPanel>
            </Border>
            <Border Style="{StaticResource KpiTile}">
                <StackPanel><TextBlock Text="دریافتی امروز" Style="{StaticResource KpiLabel}"/>
                    <TextBlock Text="{Binding TodayReceipt, StringFormat={}{0:#,##0}}" Style="{StaticResource KpiValue}" Foreground="{DynamicResource SuccessColor}"/></StackPanel>
            </Border>
            <Border Style="{StaticResource KpiTile}">
                <StackPanel><TextBlock Text="پرداختی امروز" Style="{StaticResource KpiLabel}"/>
                    <TextBlock Text="{Binding TodayPayment, StringFormat={}{0:#,##0}}" Style="{StaticResource KpiValue}" Foreground="{DynamicResource DangerColor}"/></StackPanel>
            </Border>
            <Border Style="{StaticResource KpiTile}">
                <StackPanel><TextBlock Text="موجودی صندوق" Style="{StaticResource KpiLabel}"/>
                    <TextBlock Text="{Binding CashBalance, StringFormat={}{0:#,##0}}" Style="{StaticResource KpiValue}"/></StackPanel>
            </Border>
            <Border Style="{StaticResource KpiTile}">
                <StackPanel><TextBlock Text="مانده دریافتنی" Style="{StaticResource KpiLabel}"/>
                    <TextBlock Text="{Binding Receivable, StringFormat={}{0:#,##0}}" Style="{StaticResource KpiValue}" Foreground="{DynamicResource DangerColor}"/></StackPanel>
            </Border>
        </UniformGrid>

        <!-- ═══ Dense information panels (2 × 2) ═══ -->
        <Grid Grid.Row="1" Margin="0,4,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- فاکتورهای اخیر -->
            <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource ListPanel}">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Style="{StaticResource PanelHeader}">
                        <TextBlock Text="فاکتورهای اخیر" FontWeight="Bold" FontSize="12"/></Border>
                    <telerik:RadGridView ItemsSource="{Binding RecentInvoices}" Style="{StaticResource DenseGrid}">
                        <telerik:RadGridView.Columns>
                            <telerik:GridViewDataColumn Header="شماره" DataMemberBinding="{Binding Number}" Width="90"/>
                            <telerik:GridViewDataColumn Header="مشتری" DataMemberBinding="{Binding CustomerName}" Width="*"/>
                            <telerik:GridViewDataColumn Header="مبلغ" DataMemberBinding="{Binding Amount}" DataFormatString="{}{0:#,##0}" Width="110"/>
                            <telerik:GridViewDataColumn Header="وضعیت" DataMemberBinding="{Binding Status}" Width="80"/>
                        </telerik:RadGridView.Columns>
                    </telerik:RadGridView>
                </DockPanel>
            </Border>

            <!-- چک‌های سررسید -->
            <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource ListPanel}">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Style="{StaticResource PanelHeader}">
                        <TextBlock Text="چک‌های سررسید" FontWeight="Bold" FontSize="12"/></Border>
                    <telerik:RadGridView ItemsSource="{Binding ChequesDue}" Style="{StaticResource DenseGrid}">
                        <telerik:RadGridView.Columns>
                            <telerik:GridViewDataColumn Header="شماره چک" DataMemberBinding="{Binding ChequeNumber}" Width="100"/>
                            <telerik:GridViewDataColumn Header="بانک" DataMemberBinding="{Binding BankName}" Width="*"/>
                            <telerik:GridViewDataColumn Header="مبلغ" DataMemberBinding="{Binding Amount}" DataFormatString="{}{0:#,##0}" Width="110"/>
                            <telerik:GridViewDataColumn Header="نوع" DataMemberBinding="{Binding Type}" Width="70"/>
                        </telerik:RadGridView.Columns>
                    </telerik:RadGridView>
                </DockPanel>
            </Border>

            <!-- کالاهای پرفروش -->
            <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource ListPanel}">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Style="{StaticResource PanelHeader}">
                        <TextBlock Text="کالاهای پرفروش" FontWeight="Bold" FontSize="12"/></Border>
                    <telerik:RadGridView ItemsSource="{Binding TopProducts}" Style="{StaticResource DenseGrid}">
                        <telerik:RadGridView.Columns>
                            <telerik:GridViewDataColumn Header="کالا" DataMemberBinding="{Binding Name}" Width="*"/>
                            <telerik:GridViewDataColumn Header="تعداد" DataMemberBinding="{Binding QtySold}" Width="80"/>
                            <telerik:GridViewDataColumn Header="فروش" DataMemberBinding="{Binding Revenue}" DataFormatString="{}{0:#,##0}" Width="120"/>
                        </telerik:RadGridView.Columns>
                    </telerik:RadGridView>
                </DockPanel>
            </Border>

            <!-- اعلان‌ها و هشدارها -->
            <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource ListPanel}">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Style="{StaticResource PanelHeader}">
                        <TextBlock Text="اعلان‌ها و هشدارها" FontWeight="Bold" FontSize="12"/></Border>
                    <ListBox ItemsSource="{Binding Alerts}" BorderThickness="0" Background="Transparent"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="2">
                                    <TextBlock Text="{Binding Icon}" FontSize="14" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding Message}" Margin="6,0,0,0" VerticalAlignment="Center"
                                               FontSize="12" TextWrapping="Wrap"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
