-- =============================================================================
-- U-CONSIGN-SETTLE — تسویهٔ کنسینمنت: وقتی کنسینی واقعاً کالای امانی را به مشتریِ
-- نهایی فروخت، باید بشود آن را از حسابِ «کالای امانی نزدِ دیگران» (۱-۰۵-۰۰۳) خارج
-- کرد و سندِ فروشِ واقعی (درآمد/COGS/دریافتنی) را همان لحظه زد. این ستون رهگیریِ
-- فاکتورهایِ کنسینمنتِ بازِ تسویه‌نشده را ممکن می‌کند (null = هنوز تسویه نشده).
-- =============================================================================
USE SamaHesab;
GO

IF COL_LENGTH('Sal.SalesInvoices', 'SettledVoucherId') IS NULL
BEGIN
    ALTER TABLE Sal.SalesInvoices ADD SettledVoucherId INT NULL;
END
GO
