<UserControl x:Class="SamaHesab.WPF.Views.Dashboard.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="{DynamicResource AppBackground}"
             FontFamily="{StaticResource VazirFont}"
             FlowDirection="RightToLeft">

    <UserControl.Resources>
        <Style x:Key="KpiTile" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="Padding" Value="9,5"/>
        </Style>
        <Style x:Key="KpiLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryText}"/>
        </Style>
        <Style x:Key="KpiValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="17"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,1,0,0"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>   <!-- toolbar -->
            <RowDefinition Height="Auto"/>   <!-- KPI strip -->
            <RowDefinition Height="*"/>      <!-- operational widgets -->
        </Grid.RowDefinitions>

        <!-- ═══ Compact toolbar ═══ -->
        <Border Grid.Row="0" Style="{StaticResource ErpToolbar}" Margin="3,3,3,0">
            <DockPanel>
                <TextBlock DockPanel.Dock="Right" Text="مرکز عملیات" Style="{StaticResource ErpSectionTitle}" Margin="6,0"/>
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                    <Button Style="{StaticResource ErpToolButton}" Command="{Binding RefreshCommand}">
                        <StackPanel><materialDesign:PackIcon Kind="Refresh" Width="20" Height="20" HorizontalAlignment="Center" Foreground="{DynamicResource PrimaryAccent}"/><TextBlock Text="بازخوانی" TextAlignment="Center"/></StackPanel>
                    </Button>
                    <Button Style="{StaticResource ErpToolButton}" Command="{Binding NavigateToCommand}" CommandParameter="SalesInvoice">
                        <StackPanel><materialDesign:PackIcon Kind="CartPlus" Width="20" Height="20" HorizontalAlignment="Center" Foreground="{DynamicResource SuccessColor}"/><TextBlock Text="فروش جدید" TextAlignment="Center"/></StackPanel>
                    </Button>
                    <Button Style="{StaticResource ErpToolButton}" Command="{Binding NavigateToCommand}" CommandParameter="VoucherEdit">
                        <StackPanel><materialDesign:PackIcon Kind="BookPlus" Width="20" Height="20" HorizontalAlignment="Center" Foreground="{DynamicResource PrimaryAccent}"/><TextBlock Text="سند جدید" TextAlignment="Center"/></StackPanel>
                    </Button>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- ═══ KPI strip ═══ -->
        <UniformGrid Grid.Row="1" Columns="7" Margin="0,2,0,0">
            <Border Style="{StaticResource KpiTile}"><StackPanel><TextBlock Text="فروش امروز" Style="{StaticResource KpiLabel}"/><TextBlock Text="{Binding TodaySales, StringFormat={}{0:#,##0}}" Style="{StaticResource KpiValue}" Foreground="{DynamicResource PrimaryAccent}"/></StackPanel></Border>
            <Border Style="{StaticResource KpiTile}"><StackPanel><TextBlock Text="فروش این ماه" Style="{StaticResource KpiLabel}"/><TextBlock Text="{Binding MonthSales, StringFormat={}{0:#,##0}}" Style="{StaticResource KpiValue}"/></StackPanel></Border>
            <Border Style="{StaticResource KpiTile}"><StackPanel><TextBlock Text="خرید این ماه" Style="{StaticResource KpiLabel}"/><TextBlock Text="{Binding MonthPurchase, StringFormat={}{0:#,##0}}" Style="{StaticResource KpiValue}" Foreground="{DynamicResource WarningColor}"/></StackPanel></Border>
            <Border Style="{StaticResource KpiTile}"><StackPanel><TextBlock Text="سود ماه" Style="{StaticResource KpiLabel}"/><TextBlock Text="{Binding NetProfit, StringFormat={}{0:#,##0}}" Style="{StaticResource KpiValue}" Foreground="{DynamicResource SuccessColor}"/></StackPanel></Border>
            <Border Style="{StaticResource KpiTile}"><StackPanel><TextBlock Text="مانده دریافتنی" Style="{StaticResource KpiLabel}"/><TextBlock Text="{Binding Receivable, StringFormat={}{0:#,##0}}" Style="{StaticResource KpiValue}" Foreground="{DynamicResource DangerColor}"/></StackPanel></Border>
            <Border Style="{StaticResource KpiTile}"><StackPanel><TextBlock Text="مانده پرداختنی" Style="{StaticResource KpiLabel}"/><TextBlock Text="{Binding Payable, StringFormat={}{0:#,##0}}" Style="{StaticResource KpiValue}" Foreground="{DynamicResource WarningColor}"/></StackPanel></Border>
            <Border Style="{StaticResource KpiTile}"><StackPanel><TextBlock Text="کالا کم‌موجود" Style="{StaticResource KpiLabel}"/><TextBlock Text="{Binding LowStockCount}" Style="{StaticResource KpiValue}" Foreground="{DynamicResource DangerColor}"/></StackPanel></Border>
        </UniformGrid>

        <!-- ═══ Operational widgets : 2 columns × 4 rows (wide cells, proven-safe star layout) ═══ -->
        <Grid Grid.Row="2" Margin="0,2,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/><RowDefinition Height="*"/>
                <RowDefinition Height="*"/><RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Recent Sales -->
            <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource ErpCardPanel}">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Style="{StaticResource ErpPanelHeader}">
                        <TextBlock Text="آخرین فروش‌ها" FontWeight="Bold" FontSize="12"/></Border>
                    <telerik:RadGridView ItemsSource="{Binding RecentInvoices}" Style="{StaticResource ErpGrid}" IsReadOnly="True">
                        <telerik:RadGridView.Columns>
                            <telerik:GridViewDataColumn Header="شماره" DataMemberBinding="{Binding Number}" Width="100"/>
                            <telerik:GridViewDataColumn Header="تاریخ" DataMemberBinding="{Binding Date}" Width="100"/>
                            <telerik:GridViewDataColumn Header="مشتری" DataMemberBinding="{Binding CustomerName}" Width="*"/>
                            <telerik:GridViewDataColumn Header="مبلغ" DataMemberBinding="{Binding Amount}" DataFormatString="{}{0:#,##0}" Width="130"/>
                        </telerik:RadGridView.Columns>
                    </telerik:RadGridView>
                </DockPanel>
            </Border>

            <!-- Recent Purchases -->
            <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource ErpCardPanel}">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Style="{StaticResource ErpPanelHeader}">
                        <TextBlock Text="آخرین خریدها" FontWeight="Bold" FontSize="12"/></Border>
                    <telerik:RadGridView ItemsSource="{Binding RecentPurchases}" Style="{StaticResource ErpGrid}" IsReadOnly="True">
                        <telerik:RadGridView.Columns>
                            <telerik:GridViewDataColumn Header="شماره" DataMemberBinding="{Binding Number}" Width="100"/>
                            <telerik:GridViewDataColumn Header="تاریخ" DataMemberBinding="{Binding Date}" Width="100"/>
                            <telerik:GridViewDataColumn Header="تأمین‌کننده" DataMemberBinding="{Binding CustomerName}" Width="*"/>
                            <telerik:GridViewDataColumn Header="مبلغ" DataMemberBinding="{Binding Amount}" DataFormatString="{}{0:#,##0}" Width="130"/>
                        </telerik:RadGridView.Columns>
                    </telerik:RadGridView>
                </DockPanel>
            </Border>

            <!-- Due Cheques -->
            <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource ErpCardPanel}">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Style="{StaticResource ErpPanelHeader}">
                        <TextBlock Text="چک‌های سررسید" FontWeight="Bold" FontSize="12"/></Border>
                    <telerik:RadGridView ItemsSource="{Binding ChequesDue}" Style="{StaticResource ErpGrid}" IsReadOnly="True">
                        <telerik:RadGridView.Columns>
                            <telerik:GridViewDataColumn Header="سررسید" DataMemberBinding="{Binding DueDate}" Width="100"/>
                            <telerik:GridViewDataColumn Header="بانک" DataMemberBinding="{Binding BankName}" Width="*"/>
                            <telerik:GridViewDataColumn Header="مبلغ" DataMemberBinding="{Binding Amount}" DataFormatString="{}{0:#,##0}" Width="120"/>
                            <telerik:GridViewDataColumn Header="نوع" DataMemberBinding="{Binding Type}" Width="80"/>
                        </telerik:RadGridView.Columns>
                    </telerik:RadGridView>
                </DockPanel>
            </Border>

            <!-- Low Stock -->
            <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource ErpCardPanel}">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Style="{StaticResource ErpPanelHeader}">
                        <TextBlock Text="هشدار کسری موجودی" FontWeight="Bold" FontSize="12"/></Border>
                    <telerik:RadGridView ItemsSource="{Binding LowStockItems}" Style="{StaticResource ErpGrid}" IsReadOnly="True">
                        <telerik:RadGridView.Columns>
                            <telerik:GridViewDataColumn Header="کد" DataMemberBinding="{Binding Code}" Width="90"/>
                            <telerik:GridViewDataColumn Header="کالا" DataMemberBinding="{Binding Name}" Width="*"/>
                            <telerik:GridViewDataColumn Header="موجودی" DataMemberBinding="{Binding Qty}" DataFormatString="{}{0:#,##0.##}" Width="100"/>
                            <telerik:GridViewDataColumn Header="حداقل" DataMemberBinding="{Binding MinStock}" DataFormatString="{}{0:#,##0.##}" Width="90"/>
                        </telerik:RadGridView.Columns>
                    </telerik:RadGridView>
                </DockPanel>
            </Border>

            <!-- Debtors -->
            <Border Grid.Row="2" Grid.Column="0" Style="{StaticResource ErpCardPanel}">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Style="{StaticResource ErpPanelHeader}">
                        <TextBlock Text="بدهکاران (دریافتنی)" FontWeight="Bold" FontSize="12"/></Border>
                    <telerik:RadGridView ItemsSource="{Binding Debtors}" Style="{StaticResource ErpGrid}" IsReadOnly="True">
                        <telerik:RadGridView.Columns>
                            <telerik:GridViewDataColumn Header="مشتری" DataMemberBinding="{Binding Name}" Width="*"/>
                            <telerik:GridViewDataColumn Header="مانده" DataMemberBinding="{Binding Balance}" DataFormatString="{}{0:#,##0}" Width="150"/>
                        </telerik:RadGridView.Columns>
                    </telerik:RadGridView>
                </DockPanel>
            </Border>

            <!-- Creditors -->
            <Border Grid.Row="2" Grid.Column="1" Style="{StaticResource ErpCardPanel}">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Style="{StaticResource ErpPanelHeader}">
                        <TextBlock Text="بستانکاران (پرداختنی)" FontWeight="Bold" FontSize="12"/></Border>
                    <telerik:RadGridView ItemsSource="{Binding Creditors}" Style="{StaticResource ErpGrid}" IsReadOnly="True">
                        <telerik:RadGridView.Columns>
                            <telerik:GridViewDataColumn Header="تأمین‌کننده" DataMemberBinding="{Binding Name}" Width="*"/>
                            <telerik:GridViewDataColumn Header="مانده" DataMemberBinding="{Binding Balance}" DataFormatString="{}{0:#,##0}" Width="150"/>
                        </telerik:RadGridView.Columns>
                    </telerik:RadGridView>
                </DockPanel>
            </Border>

            <!-- Top Customers -->
            <Border Grid.Row="3" Grid.Column="0" Style="{StaticResource ErpCardPanel}">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Style="{StaticResource ErpPanelHeader}">
                        <TextBlock Text="مشتریان برتر" FontWeight="Bold" FontSize="12"/></Border>
                    <telerik:RadGridView ItemsSource="{Binding TopCustomers}" Style="{StaticResource ErpGrid}" IsReadOnly="True">
                        <telerik:RadGridView.Columns>
                            <telerik:GridViewDataColumn Header="مشتری" DataMemberBinding="{Binding Name}" Width="*"/>
                            <telerik:GridViewDataColumn Header="مانده حساب" DataMemberBinding="{Binding Balance}" DataFormatString="{}{0:#,##0}" Width="150"/>
                        </telerik:RadGridView.Columns>
                    </telerik:RadGridView>
                </DockPanel>
            </Border>

            <!-- Alerts -->
            <Border Grid.Row="3" Grid.Column="1" Style="{StaticResource ErpCardPanel}">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Style="{StaticResource ErpPanelHeader}">
                        <TextBlock Text="اعلان‌ها و هشدارها" FontWeight="Bold" FontSize="12"/></Border>
                    <ListBox ItemsSource="{Binding Alerts}" BorderThickness="0" Background="Transparent"
                             HorizontalContentAlignment="Stretch"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="2,3">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding Icon}" FontSize="14" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Message}" Margin="6,0,0,0" VerticalAlignment="Center"
                                               FontSize="12" TextWrapping="Wrap"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
