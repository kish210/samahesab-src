-- =============================================================================
-- U-INV-DISC — تخفیفِ کلِ فاکتور (سرفاکتور) قبلاً فقط در سندِ حسابداری اعمال می‌شد و
-- هرگز روی خودِ فاکتور (GrandTotal/RemainAmount) ذخیره/اعمال نمی‌شد — یعنی هر فاکتورِ
-- فروشی که با تخفیفِ کلی صادر و کامل پرداخت می‌شد، یک ماندهٔ شبح‌وار به‌اندازهٔ تخفیف
-- باقی می‌گذاشت (RemainAmount اشتباه، تخصیصِ پرداختِ آینده هم می‌توانست اشتباه بیفتد).
-- این ستون تخفیفِ سرفاکتور را رویِ خودِ رکوردِ فاکتور ذخیره می‌کند.
-- توجه: فاکتورهایِ تاریخیِ پیش از این رفع که تخفیفِ سرفاکتور داشته‌اند، قابلِ اصلاحِ خودکار
-- نیستند (مبلغِ تخفیفِ اصلی هیچ‌جا ذخیره نشده بود) — فقط فاکتورهایِ جدید از این پس درست‌اند.
-- =============================================================================
USE SamaHesab;
GO

IF COL_LENGTH('Sal.SalesInvoices', 'InvoiceDiscount') IS NULL
BEGIN
    ALTER TABLE Sal.SalesInvoices ADD InvoiceDiscount DECIMAL(18,2) NOT NULL DEFAULT 0;
END
GO
